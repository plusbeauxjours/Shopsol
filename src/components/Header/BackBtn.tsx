import React from 'react';
import styled from 'styled-components/native';
import {BackIcon} from '~/constants/Icons';

const BackBtn: React.FC = () => <BackIcon size={28} color={'white'} />;

export default BackBtn;
